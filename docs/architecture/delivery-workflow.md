# Branch and Delivery Workflow

1. Work on a non-`main` task branch.
2. Ship coherent milestone commits with focused scope.
3. Run relevant lint, typecheck, test, and build commands.
4. Update progress, open issues, review notes, and PR draft artifacts.
5. Push branch when a remote exists; otherwise document the blocker and maintain `docs/pr/`.
