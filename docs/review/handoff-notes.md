# Handoff Notes

- Current branch: `work`.
- Remote push is blocked because no `origin` remote exists in this environment.
- PR draft is maintained under `docs/pr/` and mirrored via the `make_pr` tool after commit.
- Next implementation priority is persistence + migrations, then fixture-backed end-to-end ingestion/validation.
